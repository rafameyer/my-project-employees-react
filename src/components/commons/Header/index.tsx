import React from 'react';
import { Header as HeaderTitle, HeaderStruct } from './Styles';

function Header() {
    return (
        <HeaderStruct>
            <HeaderTitle>CSW Employees & Projects</HeaderTitle>
        </HeaderStruct>
    );
}

export default Header;
